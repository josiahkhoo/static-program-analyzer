#ifndef SPA_ENTITY_TYPE_H
#define SPA_ENTITY_TYPE_H

enum EntityType {
    PROCEDURE,
    STATEMENT,
    READ,
    PRINT,
    ASSIGN,
    CALL,
    WHILE,
    IF,
    VARIABLE,
    CONSTANT
};

#endif  // SPA_ENTITY_TYPE_H
