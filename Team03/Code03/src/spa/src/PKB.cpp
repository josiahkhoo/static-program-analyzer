#include <stdio.h>

#include <iostream>
#include <string>
#include <vector>

using namespace std;

#include "PKB.h"

int PKB::setProcToAST(PROC p, t_node* r) { return 0; }

t_node* PKB::getRootAST(PROC p) { return nullptr; }
